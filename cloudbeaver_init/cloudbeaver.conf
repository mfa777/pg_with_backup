{
  "server": {
    "name": "CloudBeaver Server",
    "adminCredentials": {
      "user": "${CB_ADMIN_NAME:-admin}",
      "password": "${CB_ADMIN_PASSWORD:-admin}"
    }
  },
  "connections": [
    {
      "id": "postgres-local",
      "name": "PostgreSQL",
      "driver": "postgresql",
      "host": "postgres",
      "port": 5432,
      "database": "postgres",
      "user": "${POSTGRES_USER:-postgres}",
      "password": "${POSTGRES_PASSWORD:-your_very_strong_superuser_password}",
      "savePassword": true
    }
  ]
}
