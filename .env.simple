# DEPRECATED: Minimal legacy environment example. Use `env_sample` instead.
# Will be removed in Phase 2. See CLEANUP.md
# .env file for testing with simplified containers
BACKUP_MODE=sql
POSTGRES_USER=postgres
POSTGRES_PASSWORD=test_password

# Use standard postgres image for basic testing
POSTGRES_DOCKERFILE=
POSTGRES_IMAGE=postgres:17

# General settings
TZ=UTC
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# SSH testing disabled for basic test
ENABLE_SSH_SERVER=0